name: Cache Test Workflow

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: prime-cache-13a91a0
        id: prime-cache-13a91a0
        uses: actions/cache@v4
        with:
          path: node_modules
          key: cache-13a91a0

      - name: Show cache result
        run: echo "Cache hit? ${{ steps.prime-cache-13a91a0.outputs.cache-hit }}"


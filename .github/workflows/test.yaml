name: Email Action Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f1002448@ds.study.iitm.ac.in   # 👈 must match exactly
        run: echo "Hello, world!"


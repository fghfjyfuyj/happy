name: Email Step Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step with email as both name and ID
      - name: 24f1002448@ds.study.iitm.ac.in
        id: 24f1002448@ds.study.iitm.ac.in
        run: echo "Hello, world!"

name: Email Action Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f1002448@ds.study.iitm.ac.in
        id: step-email           # 👈 the ID for automated scripts
        run: echo "Hello, world!"
